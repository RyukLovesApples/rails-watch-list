<% @list.movies.each do |movie| %>
<% bookmark = Bookmark.find_by(list: @list, movie: movie)%>
<ul>
<li><%= movie.title%></li>
</ul>
<p><%=movie.overview%></p>
<p><%=bookmark.comment%></p><%= link_to "delete", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
<%end%>

<p><%=  %></p>


<%= link_to "+ add a bookmark", new_list_bookmark_path(@list)%>
